name: flutter_provider_feature_based_project_boilerplate
description: "A new Flutter project."

publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get_it: ^8.2.0
  provider: ^6.1.5+1
  connectivity_plus: ^7.0.0
  dio: ^5.9.0
  http: ^1.5.0
  shared_preferences: ^2.5.3
  fluttertoast: ^9.0.0
  path_provider: ^2.1.5
  flutter_svg: ^2.2.1
  country_code_picker: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true